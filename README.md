# stageroll-site

The website at [stageroll.co](https://stageroll.co).

```sh
yarn install
yarn dev        # http://localhost:5173
yarn build      # static build in dist/
```

Pushing to `main` builds and deploys it to GitHub Pages.
